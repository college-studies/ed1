#include <stdio.h>

int main()
{
  int firstV = 0;
  int secondV = 5;

  if (firstV > secondV)
  {
    printf("%d Is Bigger than %d\n", firstV, secondV);
  }
  else
  {
    printf("%d Is not bigger than %d\n", firstV, secondV);
  }
}